# ── Rust / Tauri build output ──
/src-tauri/target/
# Staged frontend (generated by stage-frontend.mjs at dev/build; web/ minus ROM-derived assets)
/src-tauri/frontend/

# ── Node ──
**/node_modules/

# ── Local data / logs / backups (never ship) ──
*.log
*.bak
*.bak.*
records.json
matches.json

# ── Secrets (none are in-repo; keep them out forever) ──
.env
*.env
.env.*
*.pem
*.key
secrets.json
credentials.json

# ── ROM-derived game data (BYOR — the app ships NO game data) ──
/web/test-atlas/
**/track*.bin
**/*.arc
**/*.gdi
# Skin-library sprite RENDERS = character art + palette (ROM-derived). Palette/metadata JSON is kept.
/web/skins/**/*.png

# ── Claude local overrides (permissions) ──
.claude/settings.local.json

# ── OS ──
Thumbs.db
.DS_Store

# build artifacts
target/
skinsync/target/
src-tauri/target/

# ── Internal / private — never ship to the public client repo ──
CLAUDE.md
.claude/
docs/MVC2-STEAM-EXPERT.md
docs/STEAM-FIGHTER-STRUCT-MAP.md
docs/TRAINING-DATA-WISHLIST.md
docs/COLLECTION-BOOT-RE-HANDOVER.md
docs/KOM-SURREALDB-UNIFICATION.md
docs/BETA-READINESS.md
docs/*-EXPERT*.md
docs/*-HANDOVER*.md
docs/*-CONTEXT*.md

# deploy artifacts (session)
/ghrel/
/latest.json
latest.json
src-tauri/latest.json
skinsync/tokens.json

# server side lives in the PRIVATE repo t3chnicallyinclined/metasync-server (split 2026-08-19)
skinsync/
push-gateway/
docs/RELEASE.md
docs/DISTRIBUTED-TOURNAMENT-SPECTATOR-TAPE.md
docs/REALTIME-NEXT-WORK.md
docs/SERVER-DEPLOY-HANDOVER.md
docs/TOURNAMENT-FEATURES-SPEC.md
docs/BTCPAY-ECONOMY-STUDY.md

# ROM-derived character portraits (rendered from idle_frames; served via deploy, never committed)
app/static/chars/
