# ── Rust / Tauri build output ──
/src-tauri/target/
# Staged frontend (generated by stage-frontend.mjs at dev/build; web/ minus ROM-derived assets)
/src-tauri/frontend/

# ── Node ──
**/node_modules/

# ── Local data / logs / backups (never ship) ──
*.log
*.bak
*.bak.*
records.json
matches.json

# ── Secrets (none are in-repo; keep them out forever) ──
.env
*.env
.env.*
*.pem
*.key
secrets.json
credentials.json

# ── ROM-derived game data (BYOR — the app ships NO game data) ──
/web/test-atlas/
**/track*.bin
**/*.arc
**/*.gdi
# Skin-library sprite RENDERS = character art + palette (ROM-derived). Palette/metadata JSON is kept.
/web/skins/**/*.png

# ── Tape/replay render: game-derived data (BYOR — never commit; code/docs ARE kept) ──
# Capture / replay wire + memory-snapshot formats (always game-derived)
**/*.zcst
**/*.mcrr
**/*.mctele
**/*.blk
**/*.rrtape
**/*.rr4
**/*.rdc
# Tape render inputs + rendered game pixels + ROM-derived stage/HUD assets
/web/tapecanvas/tape.json
/web/tapecanvas/tape_*.json
/web/tapecanvas/_verify_*.png
/web/tapecanvas/**/*.png
# flycast state-injection streams (game-derived per-frame fighter state)
/tools/render-replica-poc/*.gsta
/web/tapecanvas/hud/
/web/tapecanvas/stages/
/web/tapecanvas/__pycache__/
# replay-kit live captures + dumped game sprite banks
/replay-kit/fight_live.*
/replay-kit/hud_bank/
/replay-kit/hud_bank_dump.json
/replay-kit/hud_descriptors.json
/replay-kit/__pycache__/

# ── Claude local overrides (permissions) ──
.claude/settings.local.json

# ── OS ──
Thumbs.db
.DS_Store

# build artifacts
target/
skinsync/target/
src-tauri/target/

# ── Internal / private — never ship to the public client repo ──
CLAUDE.md
.claude/
docs/MVC2-STEAM-EXPERT.md
docs/STEAM-FIGHTER-STRUCT-MAP.md
docs/TRAINING-DATA-WISHLIST.md
docs/COLLECTION-BOOT-RE-HANDOVER.md
docs/KOM-SURREALDB-UNIFICATION.md
docs/BETA-READINESS.md
docs/*-EXPERT*.md
docs/*-HANDOVER*.md
docs/*-CONTEXT*.md

# deploy artifacts (session)
/ghrel/
/latest.json
latest.json
src-tauri/latest.json
skinsync/tokens.json

# server side lives in the PRIVATE repo t3chnicallyinclined/metasync-server (split 2026-08-19)
skinsync/
push-gateway/
docs/RELEASE.md
docs/DISTRIBUTED-TOURNAMENT-SPECTATOR-TAPE.md
docs/REALTIME-NEXT-WORK.md
docs/SERVER-DEPLOY-HANDOVER.md
docs/TOURNAMENT-FEATURES-SPEC.md
docs/BTCPAY-ECONOMY-STUDY.md

# d3dcap replay working images: every one is ROM-derived (rendered from captured MvC2 pixels).
# BYOR -- these are for looking at locally and must never be committed.
/d3dcap/replay/*.png

# d3dcap sequences: a burst of captured frames, ROM-derived like the .pack files.
/d3dcap/replay/*.seq

# BYOR: live game-state dumps and derived volatility maps are ROM-derived
replay-kit/blk_snapshot.bin
replay-kit/blk_hot.npy
d3dcap/replay/tcw_pages/
# per-character HUD portrait / name-plate pages ripped from game_50.arc (rip_portraits.py) -- ROM-derived, BYOR
d3dcap/replay/portraits/
d3dcap/replay/packs/
d3dcap/replay/re_map/cache/
d3dcap/replay/re_map/*.jsonl
d3dcap/replay/re_map/__pycache__/

# TTD time-travel traces + live memory images (game-derived, multi-GB) -- never commit
/d3dcap/ttd/runs/
d3dcap/replay/fullmatch/*.seq
d3dcap/replay/fullmatch/*.mp4
d3dcap/replay/fullmatch/*.log
d3dcap/replay/fullmatch/*.txt
d3dcap/replay/capgate/
d3dcap/replay/ordergate/

# receipt runner build products (the source is kept; the exe/obj are not) and its outputs live in %TEMP%rcap_runner
/d3dcap/receipt/runner/*.exe
/d3dcap/receipt/runner/*.obj
/d3dcap/receipt/runner/*.pdb
